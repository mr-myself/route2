ja:
  errors:
    format: "%{attribute}%{message}"

  activerecord:
    attributes:
      user:
        name: '名前'
        email: 'メールアドレス'
        password: 'パスワード'

    errors:
      messages:
        blank: 'を入力してください'
        present: 'は既に取得されています'
